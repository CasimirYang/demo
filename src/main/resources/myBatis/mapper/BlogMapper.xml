<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.myBatis.UserMapper">

    <select id="selectMap" resultType="map">
        select *
        from user
        where id = #{id} for update
    </select>

    <select id="selectUser" resultType="com.myBatis.UserModel">
        select *
        from user
        where id = #{id} for update
    </select>

    <select id="selectUsers" resultType="com.myBatis.UserModel">
        select * from user for update
    </select>
    
    <resultMap id="resultMap" type="com.myBatis.UserModel">
        <id property="id" column="id" />
        <result property="name" column="name"/>
        <result property="password" column="password"/>
        <result property="age" column="age"/>
    </resultMap>

    <select id="selectUser2" resultMap="resultMap">
        select *
        from user
        where id=#{id} and name=#{name} for update
    </select>

    <update id="updateUser">
        update user set
        name='44'
        where id = #{id}
    </update>
    <update id="updateUser2">
        update user set
        name='5555'
        where id = #{id}
    </update>
</mapper>